# Dexter Personal Site V2

Dark editorial software developer portfolio built with Astro, TypeScript, and plain CSS.

## Setup

Install dependencies:

```bash
npm install
```

Run locally:

```bash
npm run dev
```

Build the static site:

```bash
npm run build
```

## Cloudflare Pages

- Build command: `npm run build`
- Output directory: `dist`
